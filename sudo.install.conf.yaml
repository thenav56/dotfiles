- defaults:
    link:
      relink: true
      create: true

# TODO: maybe use etckeeper instead?
# https://wiki.archlinux.org/title/etckeeper

- link:
    # VNC. Note: Other configs are in install.conf.yaml
    /etc/tigervnc/vncserver.users:
      path: system/tigervpn/vncserver.users


- shell:
  - [ln -sf ../run/systemd/resolve/stub-resolv.conf /etc/resolv.conf, Link for resolved]
  # - [ln system/networkd/20-wired.network /etc/systemd/network/20-wired.network, Networkd wired config]
  # - [ln system/networkd/25-wireless.network /etc/systemd/network/25-wireless.network, Networkd wireless config]
  # - [ln system/resolved/dns_over_tls.conf /etc/systemd/resolved.conf.d/dns_over_tls.conf, Resolved dns config]
