- defaults:
    link:
      relink: true
      create: true

# TODO: maybe use etckeeper instead?
# https://wiki.archlinux.org/title/etckeeper

- link:
    # VNC. Note: Other configs are in install.conf.yaml
    /etc/tigervnc/vncserver.users:
      path: system/tigervpn/vncserver.users
        # Netword: https://wiki.archlinux.org/title/systemd-networkd
    # -- NOT WORKING
    # /etc/systemd/network/:
    #   path: system/networkd/
    # -- NOT WORKING
    # Resolved https://wiki.archlinux.org/title/systemd-resolved
    # /etc/systemd/resolved.conf.d/:
    #  path: system/resolved/


- shell:
  - [ln -sf ../run/systemd/resolve/stub-resolv.conf /etc/resolv.conf, Link for resolved]
