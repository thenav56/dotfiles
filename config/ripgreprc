# Don't let ripgrep vomit really long lines to my terminal.
--max-columns=150

# Using glob patterns to include/exclude files or folders
--glob=!{**/.venv,**/.git,**/node_modules,**/coverage,**/.cache,*vim/plugged,*vim/tmp,**/.tox,**/.serverless,**/.*venv,**/.wine/}/*

--glob=!*{.pyc,*.lock}
--glob=!*{**/.pytest_cache,**/mypy_cache}

# Set the colors.
--colors=line:none
--colors=line:style:bold

# Because who cares about case!?
--smart-case
--hidden
