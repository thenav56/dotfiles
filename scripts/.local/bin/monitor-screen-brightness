#!/bin/bash -x

# TO list devices
# sudo ddcutil detect

modprobe i2c-dev

MONITOR_IDS=$(ddcutil detect | grep I2C | awk '{print($3)}' | cut -d'-' -f2)

for id in $MONITOR_IDS
do
    ddcutil --bus $id setvcp 10 $1 & # monitor brightness
done

# light -S $(expr $(expr $1 - 30) | 1)
